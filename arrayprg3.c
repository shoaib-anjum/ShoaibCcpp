/*
Read n number from user and store them into an array, 
also create another array with square root of numbers from first array
*/

#include<stdio.h>
int main()
{
    int i,n[100];
    printf("Enter the numbers: ");
    scanf("%d",&n[i]);
    printf("Numbers: %d",n[i]);

    
    
    return 0;
}